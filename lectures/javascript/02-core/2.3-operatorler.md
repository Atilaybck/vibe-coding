## 2.3 Operatörler

**Operatörler**, değerler üzerinde işlem yapmanı sağlar.

JavaScript’te en sık kullanılanlar şunlardır:

---

### 1) Aritmetik operatörler (matematik)

```jsx
+// toplama
-// çıkarma
*// çarpma
/  // bölme
%// kalan
```

```jsx
10 +2;// 12
10 %3;// 1
```

---

### 2) Karşılaştırma operatörleri

Değerleri karşılaştırır, **boolean** döner.

```jsx
>   <   >=   <=
==  !=
=== !==
```

```jsx
5 >3;// true

```

---

### `==` vs `===` (çok önemli)

```jsx
5 =="5";// true
5 ==="5";// false
```

- `==` → **tip dönüşümü yapar** (riskli)
- `===` → **tip + değer karşılaştırır** (güvenli)

👉 **Her zaman `===` kullan**

---

### 3) Mantıksal operatörler

### `&&` (AND)

Hepsi doğruysa true.

```jsx
true &&false;// false
```

### `||` (OR)

Biri doğruysa true.

```jsx
false ||true;// true
```

### `!` (NOT)

Tersine çevirir.

```jsx
!true;// false
```

---

### Modern operatörler

### `??` (Nullish coalescing)

Sadece **null veya undefined** ise sağdaki değeri alır.

```jsx
let name =null;
name ??"Guest";// "Guest"
```

**Örnek:**

```jsx
let email = undefined;

let result = email ?? "E-posta girilmedi";

console.log(result); // "E-posta girilmedi"
```

---

### `?.` (Optional chaining)

Değer yoksa hata fırlatmaz.

```jsx
user?.profile?.name;
```

**Örnek:**

```jsx
**Örnek1:**
let user = null;

let name = user?.name;

console.log(name); // undefined (hata yok)

**Örnek2:**
let settings = undefined;

let theme = settings?.theme;

console.log(theme); // undefined
```

---

### Kısa özet

- Aritmetik → matematik
- Karşılaştırma → true / false
- `===` → standart
- `&& || !` → mantık
- `??` → null/undefined kontrolü
- `?.` → güvenli erişim
### 2.5 String ve Number Pratikleri

Bu bölümde JavaScript’te **string (metin)** ve **number (sayı)** ile günlük hayatta en çok kullanılan pratikleri öğrenirsin.

---

## Template Literals (```)

String birleştirmenin **modern ve okunabilir** yoludur.

```jsx
const name ="Atılay";
const age =25;

`Merhaba ${name}, yaşın${age}`;
```

Avantajları:

- Değişkenleri kolay ekleme
- Uzun string’leri okunabilir yazma
- Alt satıra geçme desteği

```jsx
`Bu bir
çok satırlı
metindir`;
```

---

## `toFixed()`

Bir sayıyı **belirli ondalık basamakta** gösterir.

⚠️ **String döndürür**

```jsx
const price =12.3456;

price.toFixed(2);// "12.35"
```

Kullanım:

- fiyat
- para
- oran gösterimi

---

## `parseInt()`

String’i **tam sayıya** çevirir.

```jsx
parseInt("42");// 42
parseInt("42px");// 42
parseInt("3.9");// 3
```

👉 Ondalık kısmı **atar**

---

## `parseFloat()`

String’i **ondalıklı sayıya** çevirir.

```jsx
parseFloat("3.14");// 3.14
parseFloat("10.5px");// 10.5
```

---

## Küçük ama önemli fark

```jsx
Number("10.5");// 10.5
parseInt("10.5");// 10
```

---

## Ne zaman hangisi?

- Metin içine değişken → **template literals**
- Para / oran gösterimi → **toFixed**
- String → tam sayı → **parseInt**
- String → ondalıklı sayı → **parseFloat**

---

### Kısa özet

- Template literals = temiz string birleştirme
- `toFixed` → string döndürür
- `parseInt` → tam sayı
- `parseFloat` → ondalıklı sayı
## 1.2 Kurulum

JavaScript yazmak için ekstra bir şey **öğrenmeden önce** doğru ortamı kurmak gerekir.

Bu kurulum hem **tarayıcı**, hem **Node.js**, hem de **proje disiplini** içindir.

---

### 1) Node.js kurulumu

Node.js, JavaScript’i bilgisayarında çalıştırmanı sağlar.

- Node.js → JavaScript runtime (JavaScript runtime, JavaScript kodlarını tarayıcıya ihtiyaç duymadan, doğrudan bilgisayarında çalıştırmanı sağlayan çalışma ortamıdır)
- npm → paket yöneticisi (Node.js ile birlikte gelir) (Paket yöneticisi, başkaları tarafından yazılmış hazır JavaScript kütüphanelerini indirmeni, güncellemeni ve projede yönetmeni sağlayan araçtır)

Yükleme sonrası:

- `node -v` → Node sürümü
- `npm -v` → npm sürümü

> Artık JS dosyalarını terminalden çalıştırabilirsin.
> 

---

### 2) Proje başlatma (npm init)

Her ciddi JS projesi bir yapı ile başlar.

- `npm init` veya `npm init -y`
- `package.json` oluşur
- Projenin kimliği, script’leri ve bağımlılıkları burada tutulur.
    
    (Script’ler, projeyi çalıştırmak, build almak, test etmek gibi işlemler için tanımlanan hazır komutlardır) 
    
    (Bağımlılıklar, projenin çalışması için dışarıdan kullanılan hazır kütüphaneler / paketlerdir)
    

`package.json`, projenin kimlik kartı + kontrol panelidir.

- Proje bilgisi
    
    (projenin adı, versiyonu, açıklaması gibi temel bilgiler)
    
- Kütüphaneler
    
    (projede kullanılan dış paketlerin listesi; npm bu listeye bakarak neyin gerekli olduğunu bilir)
    
- Çalıştırma komutları
    
    (`npm start`, `npm run dev` gibi tek komutla projeyi çalıştırmanı sağlayan hazır talimatlar)
    

---

### 3) Editör: VS Code

JavaScript için en yaygın editör.

Temel eklentiler:

- **ESLint** → kod kalitesi (kodda hata çıkarabilecek, yanlış veya kötü yazım alışkanlıklarını tespit edip seni uyaran araçtır)
- **Prettier** → otomatik formatlama (yazdığın kodun boşluklarını, satır aralarını, girintilerini ve süslü parantez düzenini otomatik olarak düzeltir)
- **Error Lens** → hataları net gösterir (kodun içindeki hataları ve uyarıları satırın üzerinde, renkli ve açık şekilde gösterir)

> Editör = yazılım hızını belirler
> 

---

### 4) Kod formatı: Prettier

- Kodun görünümünü otomatik düzeltir
- Takım çalışmasında standart sağlar
- Tartışmayı bitirir 😄

Amaç:

- Herkes aynı formatta kod yazar
- Okunabilirlik artar

---

### 5) Kod kuralları: ESLint

- Hatalı ve riskli kodları yakalar
- Best practice uyarıları verir (Best practice, zamanla tecrübe edilerek en doğru, güvenli ve sürdürülebilir olduğu kabul edilen kod yazma yöntemleri)
- “Bu kod çalışır ama yanlış” durumlarını gösterir

**Örnek:**

- kullanılmayan değişken
- yanlış scope
- hatalı `==` kullanımı

---

### 6) Tarayıcı geliştirici araçları

JavaScript’in en iyi öğrenme aracı.

- Console
- Network
- Sources (debug)
- Breakpoint

> console.log = başlangıç
> 
> 
> Debugger = seviye atlama
> 

---

### 7) Çalışma düzeni (çok önemli)

Önerilen sistem:

- konu öğren
- 5–10 örnek yaz
- 5–10 alıştırma çöz
- mini proje yap

Bu düzen olmadan JS öğrenilmez.

---

### Kısa özet

- Node.js → JS’i bilgisayarda çalıştırır
- npm → paket yönetimi
- VS Code → yazım ortamı
- Prettier → format
- ESLint → kalite
- Tarayıcı tools → debug
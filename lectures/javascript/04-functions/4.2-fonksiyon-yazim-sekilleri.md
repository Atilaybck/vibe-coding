# 4.2 Fonksiyon Yazım Şekilleri

Bu bölümde şunu öğreneceksin:

👉 **JavaScript’te aynı işi yapan fonksiyonlar farklı şekillerde nasıl yazılır ve aralarındaki farklar nedir?**

---

## 1) Function Declaration (Klasik fonksiyon)

En temel ve en net yazım şeklidir.

Yeni başlayanlar için **en anlaşılır** olandır.

```jsx
function greet() {
console.log("Merhaba");
}

greet();
```

**Ne oluyor?**

- `function` ile fonksiyon tanımlanır
- İsim verilir (`greet`)
- Sonra `greet()` ile çağrılır

📌 **Önemli özellik (hoisting):**

Bu tür fonksiyonlar, **tanımlanmadan önce bile çağrılabilir.**

```jsx
say Hi();

function sayHi() {
console.log("Hi");
}
```

---

## 2) Function Expression (Değişkene atanan fonksiyon)

Fonksiyon bir değişkenin içine yazılır.

```jsx
const greet = function () {
console.log("Merhaba");
};

greet();
```

**Burada fark ne?**

- Fonksiyonun adı yok (anonymous function)
- Bir değişkene atanıyor (`const greet`)
- Değişken üzerinden çağrılıyor

📌 **Önemli fark:**

Bunu **tanımlamadan önce çağırırsan hata alırsın.**

```jsx
// greet(); ❌ hata

const greet =function () {
console.log("Merhaba");
};
```

👉 Günlük projelerde genelde **bu yöntem tercih edilir**, çünkü kontrol daha nettir.

---

## 3) Arrow Function (`=>`)

Arrow function, function expression’ın **daha kısa ve modern** hâlidir.

### Normal function expression:

```jsx
const topla = function (a, b) {
return a + b;
};
```

### Arrow function:

```jsx
const topla = (a, b) => {
return a + b;
};

```

### Tek satırsa daha da kısalır:

```jsx
consttopla = (a, b) => a + b;
```

**Okunuşu:**

> “a ve b al, a + b döndür”
> 

📌 Yeni başlayan için kural:

- **Tek satır → kısa arrow**
- **Uzunsa → süslü parantezli arrow**

---

## 4) Default Parametreler

Fonksiyona argüman verilmezse, **varsayılan değer** kullanılır.

```jsx
function greet(name = "Misafir") {
console.log("Merhaba " + name);
}

greet("Atılay");// Merhaba Atılay
greet();// Merhaba Misafir
```

Arrow function ile de aynıdır:

```jsx
const greet = (name = "Misafir") => {
console.log("Merhaba " + name);
};
```

**Ne işe yarar?**

- `undefined` hatalarını önler
- Fonksiyonları daha güvenli yapar

---

## 🔑 Kısa özet (akılda kalsın)

- **Function Declaration** → klasik, hoisting var
- **Function Expression** → değişkene atanır, daha kontrollü
- **Arrow Function** → kısa, modern, çok kullanılır
- **Default parametre** → değer gelmezse sorun çıkmaz
## 2.1 Değişkenler

**Değişken**, program içinde kullanılan veriyi saklayan **isimlendirilmiş alanlardır**.

Programlama temelde şudur: **veriyi tutmak, gerektiğinde değiştirmek ve kullanmak**.

---

### Değişken tanımlama

JavaScript’te 3 farklı değişken tanımlama yolu vardır.

---

### `var`

- Eski yöntemdir
- **Function scope** ile çalışır
- Hoisting nedeniyle kafa karıştırır
- Beklenmedik hatalara yol açabilir

👉 **Modern JavaScript’te kullanılmaz**

---

### `let`

- Değeri sonradan **değiştirilebilir**
- **Block scope**’a sahiptir (`{ }` içinde geçerli)

```jsx
let age =25;
age =26;// ✔
```

---

### `const`

- Değere **yeniden atama yapılamaz**
- **Block scope**’a sahiptir

```jsx
const name ="Atılay";
// name = "Ali" ❌
```

> const = sabit referans
> 
> 
> İçerik değişebilir, referans değişemez
> 

---

### Reassignment vs Mutation

### Reassignment (yeniden atama)

Değişkenin kendisine **yeni bir değer vermek**.

```jsx
let x =5;
x =10;// ✔

const y =5;
y =10;// ❌
```

---

### Mutation (içeriği değiştirme)

Object veya array’in **içindeki veriyi değiştirmek**.

```jsx
const user = {name:"Atılay" };
user.name ="Berke";// ✔

```

- `const` ile tanımlı object/array **değiştirilebilir**
- Ama tamamen yeni bir object atanamaz

---

## Scope (Kapsam)

### Block Scope (`let`, `const`)

Sadece bulunduğu `{ }` içinde geçerlidir.

```jsx
if (true) {
let a =10;
}
// a ❌
```

**Açıklama:**

`a` değişkeni sadece `if` bloğunun (`{ }`) içinde geçerlidir.
Blok dışına çıkıldığında a artık yoktur, bu yüzden erişilemez.

---

### Function Scope (`var`)

Bloğun dışına sızabilir.

```jsx
if (true) {
var b =10;
}
b;// ✔
```

**Açıklama:**

**`var` block scope değildir, function scope’tur.**

Bu yüzden `if` bloğu bitse bile `b` değişkenine blok dışından erişilebilir.

👉 Bu yüzden `var` **risklidir**

---

## Değişken isimlendirme kuralları

- Anlamlı isimler kullan
- `camelCase` tercih et
- Sayı ile başlamaz
- Özel karakter kullanılmaz (`_` ve `$` hariç)

```jsx
let userName;
let totalPrice;
```

---

### Hangi durumda hangisi?

- **Varsayılan:** `const`
- **Değişecekse:** `let`
- **`var`:** kullanma

---

### Kısa özet

- Değişken = veri tutar
- `const` → ilk tercih
- `let` → değişecek veriler
- `var` → modern JS’te kaçınılır